# Disable sudo to speed up the build
sudo: false

language: python

python:
  - "3.7"

install:
  # install dependencies.
  pip install --upgrade pip setuptools wheel
  pip install -e .

script:
  - pytest